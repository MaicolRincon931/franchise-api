version: '3.8'

services:
  franchise-api:
    build: .
    container_name: franchise-api
    ports:
      - "8080:8080"
    environment:
      - MONGODB_URI=mongodb+srv://franchiseUser:eMLG613CYRdQKtan@franchise-api.szexlgq.mongodb.net/franchise_db?appName=franchise-api
    restart: always
